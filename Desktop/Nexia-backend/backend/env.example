'''
# Environment Configuration
ENVIRONMENT=development
DEBUG=True

# Database Configuration
#DATABASE_URL=sqlite:///./nexia.db
# For PostgreSQL: postgresql://user:password@localhost/nexia_db
# For MySQL: mysql://user:password@localhost/nexia_db

# Security Configuration
SECRET_KEY=your-super-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
PASSWORD_RESET_EXPIRE_MINUTES=15
EMAIL_VERIFICATION_EXPIRE_HOURS=24

# CORS Configuration
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080"]

# Logging Configuration
LOG_LEVEL=INFO

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# Pagination Configuration
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# SMTP (Email) — optional for password reset emails
#SMTP_HOST=smtp.yourprovider.com
#SMTP_PORT=587
#SMTP_USERNAME=your_smtp_user
#SMTP_PASSWORD=your_smtp_password
#SMTP_SENDER=Your App <no-reply@yourdomain.com>
#SMTP_USE_TLS=true

# Frontend URLs
# Used to build password reset links in emails
#FRONTEND_RESET_URL=https://your-frontend.com/reset-password
# Used to build email verification links in emails
#FRONTEND_VERIFICATION_URL=https://your-frontend.com/verify-email

# Production Settings (uncomment for production)
# ENVIRONMENT=production
# DEBUG=False
# SECRET_KEY=your-production-secret-key
# BACKEND_CORS_ORIGINS=["https://yourdomain.com"]
'''
